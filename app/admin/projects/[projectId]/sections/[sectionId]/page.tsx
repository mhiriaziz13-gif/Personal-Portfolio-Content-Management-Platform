import { notFound } from "next/navigation"; import { ProjectWorkspace } from "@/components/admin/project-workspace"; import { getAdminProjectWorkspace } from "@/lib/admin-project-workspace";
export const dynamic="force-dynamic"; export default async function SectionPage({params}:{params:Promise<{projectId:string;sectionId:string}>}){const {projectId,sectionId}=await params;const data=await getAdminProjectWorkspace(projectId);if(!data.project||!data.sections.some((section)=>section.id===sectionId))notFound();return <ProjectWorkspace {...data} project={data.project} mode="section" selectedSectionId={sectionId}/>;}
